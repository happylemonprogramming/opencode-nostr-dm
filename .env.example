# ===========================================
# NOSTR PLUGIN CONFIGURATION
# ===========================================

# Required: Your server's Nostr private key (nsec1... or hex format)
# Generate with: nak key generate
NOSTR_PRIVATE_KEY=nsec1...

# Optional: Comma-separated relay URLs
# Default: wss://relay.primal.net,wss://nos.lol,wss://relay.damus.io
NOSTR_RELAYS=wss://relay.primal.net,wss://nos.lol,wss://relay.damus.io

# Optional: Enable debug logging (true/false)
# Default: false
NOSTR_DEBUG=false

# Optional: Session timeout in hours
# Default: 24
NOSTR_SESSION_TIMEOUT_HOURS=24

# Optional: Whitelist sender pubkeys (comma-separated, empty = allow all)
# Use npub format or hex pubkeys
# Default: (empty - allow all)
NOSTR_ALLOWED_SENDERS=

# ===========================================
# AI PROVIDER API KEYS (OPTIONAL)
# ===========================================
# OpenCode includes free models (Big Pickle, Kimi K2.5, etc.) that work
# without API keys. Set these only if you want to use premium models.

# Anthropic (Claude models)
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (GPT models)
# OPENAI_API_KEY=sk-...

# OpenRouter (access to many models)
# OPENROUTER_API_KEY=sk-or-...
